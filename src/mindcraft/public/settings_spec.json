{
    "profile": {
        "type": "object",
        "required": true,
        "description": "The profile object to use, including name, prompts, and examples"
    },
    "minecraft_version": {
        "type": "string",
        "description": "The version of Minecraft to use. Set to 'auto' to automatically detect the version.",
        "default": "auto"
    },
    "host": {
        "type": "string",
        "description": "The minecraft server host address to connect to",
        "default": "127.0.0.1"
    },
    "port": {
        "type": "number",
        "description": "The minecraft server port to connect to. -1 for auto-detect.",
        "default": 55916
    },
    "mindserver_host_public": {
        "type": "boolean",
        "description": "Whether to bind MindServer to 0.0.0.0 instead of loopback. Enable this for Docker/reverse-proxy deployments.",
        "default": false
    },
    "auth": {
        "type": "string",
        "description": "The authentication method to use",
        "default": "offline",
        "options": ["offline", "microsoft"]
    },
    "base_profile": {
        "type": "string",
        "description": "Allowed values: survival, assistant, creative, god_mode. Each has fine tuned settings for different game modes.",
        "default": "survival",
        "options": ["survival", "assistant", "creative", "god_mode"]
    },
    "load_memory": {
        "type": "boolean",
        "description": "Whether to load bot's previous memory",
        "default": false
    },
    "init_message": {
        "type": "string",
        "description": "The initial message to send to the bot",
        "default": "Respond with hello world and your name"
    },
    "only_chat_with": {
        "type": "array",
        "description": "List of agents to only chat with. If empty, the bot will chat publicly",
        "default": []
    },
    "speak": {
        "type": "boolean",
        "description": "Whether to enable text-to-speech reading on the host machine",
        "default": false
    },
    "voice_output_mode": {
        "type": "string",
        "description": "语音输出位置。system：使用操作系统自带朗读；local_player：在当前运行 Mindcraft 的机器本地播放；minecraft_voicechat：发送到 Paper + Simple Voice Chat + mindcraft-voice-bridge，在游戏里播放",
        "default": "local_player",
        "options": ["system", "local_player", "minecraft_voicechat"]
    },
    "voice_bridge_host": {
        "type": "string",
        "description": "仅在 minecraft_voicechat 模式下使用；Paper 语音桥接插件监听地址",
        "default": "127.0.0.1"
    },
    "voice_bridge_port": {
        "type": "number",
        "description": "仅在 minecraft_voicechat 模式下使用；Paper 语音桥接插件监听端口",
        "default": 8787
    },
    "voice_streaming": {
        "type": "boolean",
        "description": "仅在 minecraft_voicechat 模式下使用；是否优先启用流式音频输出到语音桥接",
        "default": true
    },
    "voice_target_sample_rate": {
        "type": "number",
        "description": "仅在 minecraft_voicechat 模式下使用；语音桥接目标采样率，当前建议 48000",
        "default": 48000
    },
    "voice_input_enabled": {
        "type": "boolean",
        "description": "是否启用玩家语音输入链路：Simple Voice Chat 语音 -> STT -> bot 消息",
        "default": false
    },
    "voice_input_echo_to_chat": {
        "type": "boolean",
        "description": "是否把 STT 转写结果作为系统消息同步显示到游戏聊天栏，供玩家确认识别内容",
        "default": false
    },
    "voice_input_stt_model": {
        "type": "string",
        "description": "语音转写模型，例如 qwen/qwen3-asr-flash-realtime、qwen/qwen3-asr-flash、openai/gpt-4o-mini-transcribe",
        "default": "qwen/qwen3-asr-flash-realtime"
    },
    "voice_input_stt_url": {
        "type": "string",
        "description": "语音转写接口地址；留空时使用 provider 默认地址",
        "default": ""
    },
    "voice_input_language": {
        "type": "string",
        "description": "语音转写语言，例如 en、zh；需要中英文混合输入时可设置为 auto",
        "default": "en"
    },
    "voice_input_prompt": {
        "type": "string",
        "description": "给 STT 的附加提示，用于约束转写风格，例如“不要翻译，不要加说话人标签”；当前 qwen/qwen3-asr-flash 兼容接口会忽略该字段",
        "default": "请直接转写玩家语音内容，不要翻译，不要添加说话人标签，不要补充解释。"
    },
    "voice_input_end_silence_ms": {
        "type": "number",
        "description": "判定一句话结束前需要保持静默的毫秒数，值越大越不容易过早触发",
        "default": 450
    },
    "voice_input_min_duration_ms": {
        "type": "number",
        "description": "小于该时长的语音片段会被丢弃，减少误触发",
        "default": 300
    },
    "voice_input_max_duration_ms": {
        "type": "number",
        "description": "单次语音片段允许的最长时长，超过会强制触发转写",
        "default": 15000
    },
    "voice_input_trigger_cooldown_ms": {
        "type": "number",
        "description": "一次转写完成后重新允许触发的冷却时间，减少连续抖动触发",
        "default": 150
    },
    "language": {
        "type": "string",
        "description": "The language to automatically translate to and from using google translate",
        "default": "en"
    },
    "allow_vision": {
        "type": "boolean",
        "description": "Whether to allow vision capabilities",
        "default": false
    },
    "blocked_actions": {
        "type": "array",
        "description": "List of actions that are blocked",
        "default": ["!checkBlueprint", "!checkBlueprintLevel", "!getBlueprint", "!getBlueprintLevel"]
    },
    "relevant_docs_count": {
        "type": "number",
        "description": "Number of relevant function documents to include in the prompt for LLM code writing",
        "default": 5
    },
    "max_messages": {
        "type": "number",
        "description": "Maximum number of recent messages to keep in context for LLM",
        "default": 15
    },
    "num_examples": {
        "type": "number",
        "description": "Number of examples to select to help prompt better LLM responses",
        "default": 2
    },
    "max_commands": {
        "type": "number",
        "description": "Maximum number of commands allowed in consecutive responses. -1 for no limit",
        "default": -1
    },
    "narrate_behavior": {
        "type": "boolean",
        "description": "Whether to openly chat automatic behavior like 'Picking up item!'",
        "default": true
    },
    "log_all_prompts": {
        "type": "boolean",
        "description": "Whether to log all prompts to file. Can be very verbose.",
        "default": false
    },
    "show_command_syntax": {
        "type": "string",
        "description": "Whether to show \"full\" command syntax, \"shortened\" command syntax, or \"none\"",
        "default": "full",
        "options": ["full", "shortened", "none"]
    },
    "chat_ingame": {
        "type": "boolean",
        "description": "Whether to show bot's chat messages in game chat",
        "default": true
    },
    "chat_bot_messages": {
        "type": "boolean",
        "description": "Whether to publicly chat messages to and from other bots",
        "default": true
    },
    "render_bot_view": {
        "type": "boolean",
        "description": "Whether to render bot view for user observation. Does not give bot vision.",
        "default": false
    },
    "allow_insecure_coding": {
        "type": "boolean",
        "description": "Whether to allow newAction command that let's LLM write/run code on host computer. Despite sandboxxing, it is potentially insecure.",
        "default": false
    },
    "code_timeout_mins": {
        "type": "number",
        "description": "Number of minutes to allow code execution. -1 for no timeout",
        "default": -1
    },
    "task": {
        "type": "object",
        "description": "The task object to give the agent on start. If null, the agent will not have a task.",
        "default": null
    },
    "spawn_timeout": {
        "type": "number",
        "description": "Number of seconds allowed for the bot to spawn before throwing an error. Increase when spawning takes a while.",
        "default": 30
    }
}
